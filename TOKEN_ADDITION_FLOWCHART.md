# 🗺️ Token Addition Flow Diagram

## 🎯 Visual Journey: Adding New Token to OVE Swap

```
┌─────────────────────────────────────────────────────────────────────┐
│                    START: Want to Add New Token                      │
└─────────────────────────┬───────────────────────────────────────────┘
                          │
                          ▼
                  ┌───────────────────┐
                  │ Gather Information│
                  └───────┬───────────┘
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
        ▼                 ▼                 ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│Token Address │  │ Token Price  │  │   Decimals   │
│   (BSC)      │  │  (Current)   │  │   (6/18)     │
└──────┬───────┘  └──────┬───────┘  └──────┬───────┘
       │                 │                 │
       └─────────────────┼─────────────────┘
                         │
                         ▼
              ┌──────────────────────┐
              │Does Chainlink Feed   │
              │   Exist for Token?   │
              └──────────┬───────────┘
                         │
         ┌───────────────┴───────────────┐
         │                               │
         ▼ YES                           ▼ NO
┌─────────────────────┐         ┌─────────────────────┐
│  METHOD 1:          │         │  METHOD 2:          │
│  Chainlink Feed     │         │  Manual Rate        │
│  (Recommended)      │         │  (Fallback)         │
└─────────┬───────────┘         └─────────┬───────────┘
          │                               │
          │                               │
          │                               ▼
          │                     ┌─────────────────────┐
          │                     │ Calculate Rate:     │
          │                     │ (TokenPrice/        │
          │                     │  OVEPrice) * 10^18  │
          │                     └─────────┬───────────┘
          │                               │
          └───────────────┬───────────────┘
                          │
                          ▼
        ┌─────────────────────────────────────┐
        │   STEP 1: Add Token Support         │
        │   Call: addSupportedToken()         │
        │   - Token address                   │
        │   - Exchange rate (placeholder)     │
        │   - Active = true                   │
        └─────────────────┬───────────────────┘
                          │
        ┌─────────────────┴─────────────────┐
        │                                   │
        ▼ IF CHAINLINK                      ▼ IF MANUAL
┌─────────────────────┐             ┌─────────────────────┐
│  STEP 2A:           │             │  STEP 2B:           │
│  Set Price Feed     │             │  Set Manual Rate    │
│  setPriceFeed()     │             │  (Already set in    │
│  - Token address    │             │   Step 1)           │
│  - Feed address     │             │                     │
└─────────┬───────────┘             └─────────┬───────────┘
          │                                   │
          │         ┌─────────────────────────┘
          │         │
          └─────────┤
                    │
                    ▼
        ┌───────────────────────────┐
        │ STEP 3: Set OVE Price     │
        │ (if not already set)      │
        │ setOVEPriceInUSD()        │
        │ Example: 1000000 = $0.01  │
        └───────────┬───────────────┘
                    │
                    ▼
        ┌───────────────────────────────┐
        │ STEP 4A: Add Token Liquidity  │
        │                               │
        │ 1. Approve tokens to swap     │
        │    Token.approve(swap, amt)   │
        │                               │
        │ 2. Add liquidity              │
        │    swap.addLiquidity(tok,amt) │
        └───────────┬───────────────────┘
                    │
                    ▼
        ┌───────────────────────────────┐
        │ STEP 4B: Add OVE Liquidity    │
        │                               │
        │ 1. Approve OVE to swap        │
        │    OVE.approve(swap, amt)     │
        │                               │
        │ 2. Add OVE liquidity          │
        │    swap.addOVELiquidity(amt)  │
        └───────────┬───────────────────┘
                    │
                    ▼
        ┌───────────────────────────────┐
        │ STEP 5: Verification          │
        │                               │
        │ ✓ supportedTokens = true      │
        │ ✓ isActive = true             │
        │ ✓ Price info looks correct    │
        │ ✓ Liquidity is sufficient     │
        └───────────┬───────────────────┘
                    │
                    ▼
        ┌───────────────────────────────┐
        │ STEP 6: Test Swaps            │
        │                               │
        │ 1. Test buy OVE (small amt)   │
        │ 2. Test sell OVE (small amt)  │
        │ 3. Verify fees paid correctly │
        └───────────┬───────────────────┘
                    │
                    ▼
        ┌───────────────────────────────┐
        │ ✅ SUCCESS!                    │
        │ Token is now live for swaps   │
        │ Users can buy/sell OVE        │
        └───────────────────────────────┘
```

---

## 📋 Function Call Sequence

### **Scenario A: Adding USDT with Chainlink Feed**

```
┌──────────────────────────────────────────────────────┐
│ Contract Owner Actions (On TokenSwapWithNativeBNB)   │
└──────────────────────────────────────────────────────┘

1️⃣  swap.addSupportedToken(
      "0x55d39...955",        // USDT address
      100000000000000000000,  // Placeholder rate
      true                    // Active
    )
    ↓
2️⃣  swap.setPriceFeed(
      "0x55d39...955",        // USDT address
      "0xB97Ad...320"         // USDT/USD Chainlink feed
    )
    ↓
3️⃣  swap.setOVEPriceInUSD(1000000)  // $0.01
    ↓
    
┌──────────────────────────────────────────────────────┐
│ Token Approval & Liquidity (On Token Contracts)      │
└──────────────────────────────────────────────────────┘

4️⃣  USDT.approve(swapAddress, 10000 * 10^6)
    ↓
5️⃣  swap.addLiquidity(
      "0x55d39...955",  // USDT
      10000 * 10^6      // 10,000 USDT
    )
    ↓
6️⃣  OVE.approve(swapAddress, 1000000 * 10^18)
    ↓
7️⃣  swap.addOVELiquidity(1000000 * 10^18)
    ↓

┌──────────────────────────────────────────────────────┐
│ Verification (Read-only calls)                        │
└──────────────────────────────────────────────────────┘

8️⃣  swap.getPriceInfo("0x55d39...955")
    ↓
9️⃣  swap.calculateBuyOVEWithFees(
      "0x55d39...955",
      100 * 10^6  // 100 USDT
    )
    ↓
🔟 Small test swap (buy & sell)
    ↓
    
✅ DONE! Users can now swap USDT ↔ OVE
```

---

## 🎮 Quick Reference Table

| Step | Function | Required? | Who Calls |
|------|----------|-----------|-----------|
| 1 | `addSupportedToken()` | ✅ YES | Owner |
| 2a | `setPriceFeed()` | 🔵 If Chainlink | Owner |
| 2b | `updateExchangeRate()` | 🟢 If Manual | Owner |
| 3 | `setOVEPriceInUSD()` | ⚠️ Once | Owner |
| 4 | `Token.approve()` | ✅ YES | Owner |
| 5 | `addLiquidity()` | ✅ YES | Owner |
| 6 | `OVE.approve()` | ✅ YES | Owner |
| 7 | `addOVELiquidity()` | ✅ YES | Owner |
| 8 | `getPriceInfo()` | 🔍 Verify | Anyone |
| 9 | Test swap | 🧪 Recommended | Anyone |

---

## 🔄 State Transitions

```
                    INITIAL STATE
                         │
                         │ addSupportedToken()
                         ▼
              ┌──────────────────────┐
              │ supportedTokens[tok] │
              │      = true          │
              │                      │
              │ exchangeRates[tok]   │
              │      = rate          │
              │                      │
              │ isActive[tok]        │
              │      = true          │
              └──────────┬───────────┘
                         │
                         │ setPriceFeed() [optional]
                         ▼
              ┌──────────────────────┐
              │ hasCustomPriceFeed   │
              │      = true          │
              │                      │
              │ priceFeeds[tok]      │
              │      = feedAddress   │
              └──────────┬───────────┘
                         │
                         │ addLiquidity()
                         ▼
              ┌──────────────────────┐
              │ Contract holds:      │
              │  - Token balance     │
              │  - OVE balance       │
              └──────────┬───────────┘
                         │
                         ▼
                 🎉 READY TO SWAP!
                         │
         ┌───────────────┼───────────────┐
         │                               │
         ▼                               ▼
  ┌─────────────┐              ┌─────────────┐
  │  buyOVE()   │              │  sellOVE()  │
  │             │              │             │
  │ Token → OVE │              │ OVE → Token │
  └─────────────┘              └─────────────┘
```

---

## 💡 Pro Tips

### ✅ **DO:**
- Start with testnet before mainnet
- Add sufficient liquidity on both sides
- Test with small amounts first
- Monitor price feeds regularly
- Keep liquidity balanced
- Document token addresses

### ❌ **DON'T:**
- Add tokens without liquidity
- Skip verification steps
- Use wrong decimals
- Forget to approve tokens
- Set unrealistic rates
- Deploy without testing

---

## 🚨 Troubleshooting Decision Tree

```
                 Swap Transaction Failing?
                          │
          ┌───────────────┼───────────────┐
          │               │               │
          ▼               ▼               ▼
   "Not supported"  "Not active"  "Insufficient liquidity"
          │               │               │
          ▼               ▼               ▼
  addSupportedToken() unpause()    addLiquidity()
                                   addOVELiquidity()

                 Price Looks Wrong?
                          │
          ┌───────────────┼───────────────┐
          │                               │
          ▼                               ▼
    Using Chainlink?                Using Manual?
          │                               │
          ▼                               ▼
  - Check feed address          - Recalculate rate
  - Verify OVE price            - Call updateExchangeRate()
  - Check staleness             - Set OVE price
```

---

## 📞 Quick Commands Cheat Sheet

```bash
# Check if token is supported
cast call $SWAP "supportedTokens(address)(bool)" $TOKEN_ADDR

# Check if token is active
cast call $SWAP "isActive(address)(bool)" $TOKEN_ADDR

# Get price info
cast call $SWAP "getPriceInfo(address)" $TOKEN_ADDR

# Check liquidity
cast call $SWAP "getContractBalance(address)(uint256)" $TOKEN_ADDR

# Add token support
cast send $SWAP "addSupportedToken(address,uint256,bool)" \
  $TOKEN_ADDR $RATE true --private-key $KEY

# Set price feed
cast send $SWAP "setPriceFeed(address,address)" \
  $TOKEN_ADDR $FEED_ADDR --private-key $KEY

# Add liquidity
cast send $SWAP "addLiquidity(address,uint256)" \
  $TOKEN_ADDR $AMOUNT --private-key $KEY
```

---

This flowchart provides a visual guide to complement the detailed written instructions! 🎯

